 Makefile     |  2 +-
 config.def.h | 67 ++++++++++++++++++++++++++++++++++++++++++----------
 2 files changed, 55 insertions(+), 14 deletions(-)

diff --git a/Makefile b/Makefile
index 77bcbc0..c05dbdd 100644
--- a/Makefile
+++ b/Makefile
@@ -26,7 +26,7 @@ dwm: ${OBJ}
 	${CC} -o $@ ${OBJ} ${LDFLAGS}
 
 clean:
-	rm -f dwm ${OBJ} dwm-${VERSION}.tar.gz
+	rm -f dwm ${OBJ} dwm-${VERSION}.tar.gz config.h
 
 dist: clean
 	mkdir -p dwm-${VERSION}
diff --git a/config.def.h b/config.def.h
index 14d52c0..670b990 100644
--- a/config.def.h
+++ b/config.def.h
@@ -1,13 +1,17 @@
 /* See LICENSE file for copyright and license details. */
 
+#define LAPTOP 0
+
+#include <X11/XF86keysym.h>
+
 /* appearance */
 static const unsigned int borderpx  = 1;        /* border pixel of windows */
 static const unsigned int snap      = 32;       /* snap pixel */
 static const int swallowfloating    = 0;        /* 1 means swallow floating windows by default */
 static const int showbar            = 1;        /* 0 means no bar */
 static const int topbar             = 1;        /* 0 means bottom bar */
-static const char *fonts[]          = { "monospace:size=10" };
-static const char dmenufont[]       = "monospace:size=10";
+static const char *fonts[]          = { "monospace:size=11" };
+static const char dmenufont[]       = "monospace:size=11";
 static const char col_gray1[]       = "#222222";
 static const char col_gray2[]       = "#444444";
 static const char col_gray3[]       = "#bbbbbb";
@@ -29,8 +33,7 @@ static const Rule rules[] = {
 	 */
 	/* class     instance  title           tags mask  isfloating  isterminal  noswallow  monitor */
 	{ "Gimp",    NULL,     NULL,           0,         1,          0,           0,        -1 },
-	{ "Firefox", NULL,     NULL,           1 << 8,    0,          0,          -1,        -1 },
-	{ "St",      NULL,     NULL,           0,         0,          1,           0,        -1 },
+	{ "st",      NULL,     NULL,           0,         0,          1,           0,        -1 },
 	{ NULL,      NULL,     "Event Tester", 0,         0,          0,           1,        -1 }, /* xev */
 };
 
@@ -76,19 +79,20 @@ static const Key keys[] = {
 	{ MODKEY,                       XK_k,      focusstack,     {.i = -1 } },
 	{ MODKEY,                       XK_i,      incnmaster,     {.i = +1 } },
 	{ MODKEY,                       XK_d,      incnmaster,     {.i = -1 } },
-	{ MODKEY,                       XK_h,      setmfact,       {.f = -0.05} },
-	{ MODKEY,                       XK_l,      setmfact,       {.f = +0.05} },
-	{ MODKEY|ShiftMask,             XK_h,      setcfact,       {.f = +0.25} },
-	{ MODKEY|ShiftMask,             XK_l,      setcfact,       {.f = -0.25} },
+	{ MODKEY,                       XK_h,      setmfact,       {.f = -0.03} },
+	{ MODKEY,                       XK_l,      setmfact,       {.f = +0.03} },
+	{ MODKEY|ShiftMask,             XK_h,      setcfact,       {.f = +0.1} },
+	{ MODKEY|ShiftMask,             XK_l,      setcfact,       {.f = -0.1} },
 	{ MODKEY|ShiftMask,             XK_o,      setcfact,       {.f =  0.00} },
 	{ MODKEY,                       XK_Return, zoom,           {0} },
 	{ MODKEY,                       XK_Tab,    view,           {0} },
 	{ MODKEY|ShiftMask,             XK_c,      killclient,     {0} },
-	{ MODKEY,                       XK_t,      setlayout,      {.v = &layouts[0]} },
-	{ MODKEY,                       XK_f,      setlayout,      {.v = &layouts[1]} },
-	{ MODKEY,                       XK_m,      setlayout,      {.v = &layouts[2]} },
-	{ MODKEY,                       XK_u,      setlayout,      {.v = &layouts[3]} },
-	{ MODKEY,                       XK_o,      setlayout,      {.v = &layouts[4]} },
+	{ MODKEY,                       XK_t,      setlayout,      {.v = &layouts[0]} }, // tile
+	{ MODKEY,                       XK_f,      setlayout,      {.v = &layouts[1]} }, // float
+	{ MODKEY,                       XK_m,      setlayout,      {.v = &layouts[2]} }, // monocle
+	{ MODKEY,                       XK_u,      setlayout,      {.v = &layouts[3]} }, // center M
+	{ MODKEY,                       XK_o,      setlayout,      {.v = &layouts[4]} }, // center FM
+	{ MODKEY,                       XK_g,      setlayout,      {.v = &layouts[5]} }, // grid
 	{ MODKEY,                       XK_space,  setlayout,      {0} },
 	{ MODKEY|ShiftMask,             XK_space,  togglefloating, {0} },
 	{ MODKEY,                       XK_0,      view,           {.ui = ~0 } },
@@ -97,6 +101,43 @@ static const Key keys[] = {
 	{ MODKEY,                       XK_period, focusmon,       {.i = +1 } },
 	{ MODKEY|ShiftMask,             XK_comma,  tagmon,         {.i = -1 } },
 	{ MODKEY|ShiftMask,             XK_period, tagmon,         {.i = +1 } },
+
+#define WINKEY Mod4Mask
+#define COMMAND(...) spawn, { .v = (const char*[]){ __VA_ARGS__, NULL } }
+
+    { WINKEY,        XK_w,                     COMMAND("bookmark-add") },
+    { WINKEY,        XK_e,                     COMMAND("bookmark-open") },
+    { WINKEY,        XK_r,                     COMMAND("trigger-compilation") },
+    { WINKEY,        XK_F6,                    COMMAND("toggle-headphones") },
+    { WINKEY,        XK_Menu,                  COMMAND("rotate-keyboard-layout") },
+    { WINKEY,        XK_Escape,                COMMAND("swap-caps-and-escape") },
+    { WINKEY,        XK_n,                     COMMAND("show-news") },
+    { 0,             XF86XK_AudioNext,         COMMAND("media-control", "next") },
+    { 0,             XF86XK_AudioPrev,         COMMAND("media-control", "previous") },
+    { 0,             XF86XK_AudioPlay,         COMMAND("media-control", "play") },
+    { 0,             XF86XK_AudioPause,        COMMAND("media-control", "pause") },
+    { 0,             XF86XK_AudioForward,      COMMAND("media-control", "forward") },
+    { 0,             XF86XK_AudioRewind,       COMMAND("media-control", "rewind") },
+    { WINKEY,        XK_b,                     COMMAND("/usr/bin/firefox") },
+    { WINKEY,        XK_g,                     COMMAND("/usr/bin/steam") },
+    { WINKEY,        XK_l,                     COMMAND("/usr/bin/xsecurelock") },
+    { WINKEY,        XK_o,                     COMMAND("/usr/bin/libreoffice") },
+    { WINKEY,        XK_s,                     COMMAND("/usr/bin/scrot") },
+    { WINKEY|MODKEY, XK_s,                     COMMAND("/usr/bin/flameshot", "gui") },
+#if LAPTOP
+    { 0,             XF86XK_MonBrightnessUp,   COMMAND("change-brightness", "up") },
+    { 0,             XF86XK_MonBrightnessDown, COMMAND("change-brightness", "down") },
+    { 0,             XF86XK_AudioMute,         COMMAND("volume-control", "toggle") },
+    { 0,             XF86XK_AudioRaiseVolume,  COMMAND("volume-control", "up") },
+    { 0,             XF86XK_AudioLowerVolume,  COMMAND("volume-control", "down") },
+#else
+    { WINKEY,        XK_F5,                    COMMAND("volume-control", "toggle") },
+    { WINKEY,        XK_F8,                    COMMAND("volume-control", "up") },
+    { WINKEY,        XK_F7,                    COMMAND("volume-control", "down") },
+#endif
+#undef COMMAND
+#undef WINKEY
+
 	TAGKEYS(                        XK_1,                      0)
 	TAGKEYS(                        XK_2,                      1)
 	TAGKEYS(                        XK_3,                      2)
-- 
2.42.0

